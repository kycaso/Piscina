#include <stdio.h>
#include <string.h>

int		ft_strncmp(char *s1, char *s2, unsigned int n);

int main(void)
{
	char s1[]="hoooola buenas tardes";
	char s2[]= "pepe jose";
	unsigned int i;
	char *paco;

	i = 7;
	paco = strncmp(s1, s2, i)
	unsigned int i = 0;
	while( i < n)
	{
		if (s2[i] == '\0')
			printf("0");
	   	i++;
	}

}
